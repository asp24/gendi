# Basic Example

This example shows:
- parameters
- tagged injection with sorting
- decorators
- method-based constructors

Generate the container:

```
go generate ./examples/basic
```

Then use the generated package in your code (see `examples/basic/internal/di`).
